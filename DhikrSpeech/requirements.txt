# DhikrSpeech - Python 3.11
#
# Google Colab already ships TensorFlow, NumPy, scikit-learn, matplotlib and
# pandas. The notebooks only install what is actually missing, so a plain
# `pip install -r requirements.txt` is for local or non-Colab environments.

tensorflow>=2.15,<2.21
numpy>=1.26,<3.0
scipy>=1.11

librosa>=0.10.1
soundfile>=0.12.1
soxr>=0.3.7
audioread>=3.0

scikit-learn>=1.3
matplotlib>=3.7
pandas>=2.0
PyYAML>=6.0
tensorboard>=2.15
